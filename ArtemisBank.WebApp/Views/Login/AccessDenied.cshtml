@{ ViewData["Title"] = "Acceso denegado"; }

<div class="text-center py-5">
    <i class="bi bi-shield-lock display-4 text-danger"></i>
    <h1 class="h4 mt-3">Acceso denegado</h1>
    <p class="text-secondary">No tiene permisos para acceder a esta sección con su rol actual.</p>
    <a class="btn btn-primary" asp-controller="Home" asp-action="Index">Volver al inicio</a>
</div>
